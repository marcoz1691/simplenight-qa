# Copy relevant values into env/.env.<environment> or export as shell vars.
# ENV selects which env/.env.<ENV> file is loaded (staging | qa | production)
ENV=staging
BASE_URL=https://wl.stg.simplenight.com/
DEFAULT_TIMEOUT=15000
NAVIGATION_TIMEOUT=30000
HEADLESS=true
